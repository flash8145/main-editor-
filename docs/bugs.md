# Bug log — Phase A1 sweep

Working list for the A1 bug sweep (MASTERPLAN §5). One line per bug: status, where, what, fix.
Priority order: data loss > broken feature > visual.

| # | Status | Area | Bug | Fix / plan |
|---|--------|------|-----|------------|
| 1 | ✅ fixed | Editor toolbar | Project title + specs line wrapped at widths ≲1000px and overflowed the fixed-height toolbar, overlapping the panel below (owner + Claude both reproduced) | `min-w-0` on the toolbar's left flex group and title column, `truncate` on the h1 and specs span, `flex-shrink-0` on the back button (`features/editor/components/toolbar.tsx`) |
| 2 | 📋 logged → A2 | Color workspace | "+ Add Effect" button label ellipsizes to "+ A…" at 1280px; Copy/Paste grade + Adjustment layer buttons wrap to two cramped rows | Bottom-row layout of the Color workspace needs a responsive pass — part of the A2 UI overhaul, not a point fix |
| 3 | 📋 logged → A2 | Color + Animate workspaces | Large unused black band (~20% of window height) below the panel row at 1280×800 — workspace content doesn't stretch to fill the viewport | Rework workspace vertical layout during A2 (panels should flex-fill) |
| 4 | ✅ fixed | Transitions panel | Click-to-apply never worked for normal footage: linked selection (default-on) selects video+audio = 2 items, and the resolver required exactly 1 selected item, so clicking a transition card silently did nothing and the hint said "select a single clip" | `resolveTransitionTargetFromSelection` now filters selection to transitionable types (video/image/composition) before the single-item check; panel hint/gate counts the same way (`transition-targets.ts`, `transitions-panel.tsx`; regression tests added) |
| 5 | ✅ fixed | Source monitor edits | Insert/overwrite edits left dangling transitions: removed or split-away clips were never reported to `applyTransitionRepairs`, so a transition could keep referencing a deleted clip id (invisible, unrepaired, saved into the project) | `performInsertEdit`/`performOverwriteEdit` now report the stale pre-split/removed item ids as affected; repair then rewires the transition to the replacement clip at the cut or drops it (`source-edit-actions.ts`; regression tests added) |
| 6 | ✅ fixed | Left rail (workspace panels) | Panel-switcher icons (Media, Text, Shapes, Effects, Transitions, …) have no accessible names — read_page shows bare `button` entries; screen readers get nothing | Added `aria-label={label}` (reusing the existing `data-tooltip` string, already i18n-translated) + `aria-pressed` for the active-state to each category button, and `aria-label` to the sidebar collapse/expand toggle above them (`features/editor/components/media-sidebar.tsx`). Verified in-browser: read_page now reports named buttons ("Text", etc.) |
| 7 | 📋 logged | Media decode | Repeated console errors "A VideoSample was garbage collected without first being closed" during heavy source-monitor scrubbing + rapid seeking (mediabunny resource leak — GC covers it, but decoder/VRAM pressure until then) | Audit VideoSample lifecycle in the scrub/decode path; reproduce with rapid seeks in the source monitor |
| 9 | ✅ fixed | Preview (transition playback) | **Owner-reported + root-caused from `__DEBUG__.transitionTrace()` on owner's machine (spiralWipe, project uQTLsJFF).** Playback stutters so badly around the transition that it's unwatchable: one `preview_transition_session` measured **6021 ms**; rendered frame fell ~175 frames behind the clock (`currentFrame 891 / frame 716`), every frame tagged `render_frame_slow`. **Root cause: non-GPU transitions.** 21 of the 44 presentations had `gpuTransitionId: null` (all Wipe-mask variants band/center/edge/radial/spiral/venetianBlind/x, all Iris *shapes* arrow/cross/diamond/eye/hexagon/oval/pentagon/square/triangle, all Shapes box/heart/star/triangleLeft/triangleRight) and rendered through the **CPU Canvas-2D fallback** (`CanvasTransitions.renderCanvas` via the offline `ClientRenderEngine`), decoding each participant frame with mediabunny WASM (~40–80 ms/frame) instead of the fast GPU path. Preview-only — exports render offline so output was correct either way | **All 21 ported to WebGPU shaders** following the `GPU_TRANSITION_REGISTRY` pattern; CPU `renderCanvas` kept as the true no-WebGPU fallback. **Wipe-mask (7)** → `infrastructure/gpu-transitions/transitions/wipe-mask.ts` (edge/center/band/venetianBlind analytic masks; radial angle-fold; x `sdSegment`; spiral polar arm-search), linked in `renderers/wipe.ts`. **Iris-shape (9) + Shape (5)** → `infrastructure/gpu-transitions/transitions/aperture-masks.ts` — polygon apertures (arrow/cross/diamond/hexagon/pentagon/square/triangle/box/star) use a WGSL even-odd point-in-polygon test with vertices generated from the SAME formulas as the CPU renderers; oval = ellipse; eye = parabolic lens (approximation of the CPU bezier); heart = implicit `(x²+y²−1)³−x²y³` curve (approximation); triangleLeft/Right = uv-space corner triangles. Linked via `gpuTransitionId` on `createIrisRenderer`/`createShapeRenderer` in `renderers/{iris,shape}.ts`. Verified in-browser: diamond/star/cross(non-convex 12-gon)/heart/eye/triangleLeft all render correct distinct apertures (screenshotted), no shader-compile errors, and a comparative `transitionTrace()` shows crossIris/diamondIris warm-composite at **0.09 ms — identical to the known-GPU `wipe` (0.13 ms)**, confirming the GPU path (CPU-fallback would be tens of ms). Regression tests: `gpu-transitions/index.test.ts` (metadata/uniform packing for all 21) + `renderers/{wipe,iris,shape}.test.ts` (assert every variant sets `gpuTransitionId`). `npm run verify` green. Note: eye and heart are intentional visual approximations of the CPU bezier shapes — they read correctly and stay reasonably consistent with the fallback. |
| 8 | ✅ fixed | Preview (paused) | Committed item property edits don't repaint the paused preview: the fast-scrub overlay lingers showing a stale frame indefinitely; edits (and even a programmatic `seekTo`) don't clear it — only **play** does. **Root cause found: the trigger is a ruler HOVER-SKIM, not a drag.** `handleRulerMouseMove` (`features/timeline/components/timeline-markers.tsx:785`) sets `previewFrame` on mouse-move over the ruler (skim preview) but **never commits `currentFrame`**; `handleRulerMouseLeave` clears `previewFrame`→null on mouse-out. So after skimming to frame N and moving off the ruler, `previewFrame`=null but `currentFrame` is still its pre-skim value M (≠ N). The settle branch `use-preview-render-pump-controller.ts:1473-1478` (`targetFrame===null` → `if (showFastScrubOverlayRef.current)`) then seeks the DOM Player to `currentFrame` M and **returns, leaving the overlay orphaned at the skimmed frame N** — the branch assumes the visible overlay is already showing `currentFrame` (true after a drag-RELEASE that commits currentFrame=N, false after a hover-skim) and has no follow-up to redraw/hide it once the player catches up. Result: overlay shows frame N while playhead/currentFrame is M; player sits correct-but-hidden underneath. This is why last session couldn't repro — every path they tried (drag-release, forced-overlay via blend/effect, paused-in-transition) **committed** currentFrame, so overlay-frame == currentFrame and the items-change repaint effect (`use-preview-renderer-controller.ts:741-826`, targets `previewFrame ?? currentFrame`) redrew fine. **Repro (this session, authentic):** import real video → clip on V1 + text overlay → dispatch a fast sequence of bubbling `mousemove` events across `.timeline-ruler` (React ruler handler fires from real mousemove; `computer` hover/drag actions do NOT emit a React-visible mousemove, which is why prior synthetic attempts failed) → dispatch `mouseout` with `relatedTarget` outside the ruler (React synthesizes `onMouseLeave` from native mouseout+relatedTarget) → overlay left at `displayedFrame=190` while `currentFrame=4`, `previewFrame=null`, DOM video correctly at frame 4 but hidden. Verified stuck >10s across a committed `updateItem` text edit (store shows new text, overlay unchanged) and a `seekTo(30)`; cleared only by play. | **Fixed** — two defects, both required. **(1) Settle orphaned the overlay** (`use-preview-render-pump-controller.ts`, `targetFrame===null` branch): the `showFastScrubOverlayRef.current` case only seeked the Player and returned, assuming the overlay already showed `currentFrame` (true after a drag-release commit, false after a hover-skim). Now it redraws the overlay to `currentFrame` via the pump (same path as the required-rendered-overlay case; the two now-identical branches were merged behind the pure predicate `utils/scrub-settle-overlay.ts::shouldRedrawSettledScrubOverlay`). **(2) Committed edits didn't repaint** (`use-preview-renderer-controller.ts`, items-change invalidation effect ~798): instrumentation confirmed the effect fired with all conditions true (`structureMatch`/`currentFrameInvalidated`/`overlayVisibleGate`) and called `resumeScrubLoopRef.current()` — but **no pump ran**, because a committed edit re-runs the render-pump `useEffect`, whose cleanup sets `resumeScrubLoopRef` to a no-op before the re-run restores it, so the **synchronous** kick was silently dropped. Fixed by deferring the kick to a `queueMicrotask` (matching the existing deferral in the renderer-dispose effect ~722). Concurrency invariants untouched (no mutex/generation changes). Verified end-to-end in the Browser pane: hover-skim→mouseleave now settles to overlay-frame == `currentFrame` (was 190 vs 4), and a committed text edit repaints the paused overlay (`BEFORE`→`AFTER-EDIT` screenshotted); play/pause, normal seek, and normal paused edits (DOM-Player path) unaffected. Tests: `utils/scrub-settle-overlay.test.ts` (predicate) + a resume-kick-race simulation in `utils/render-pump-invariants.test.ts`. `npm run verify` green. |

| 10 | ✅ fixed | Audio EQ panel | Per-band number inputs (frequency/gain/Q) are unreadable: the EQ lays 6 bands in a fixed `grid grid-cols-6` at **36px/cell** inside a ~236px column, leaving each input **~14px wide** — multi-digit values clip to one digit (`22000`→"2", `1600`→"3"). Values are correct in the store and the commit path works (verified: typing 45 into band-1 freq wrote `audioEqBand1FrequencyHz:45`); it's purely that you can't read/edit them | Changed the narrow-sidebar `CompactBandRows` layout (toggle/filter-type/frequency/gain/Q rows) from `grid-cols-6` to `grid-cols-3` — the ~236px column's 6 bands now wrap to 2 rows of 3, roughly doubling per-cell width (~33px→~70px), enough for the unit suffix + full multi-digit value (`features/editor/components/properties-sidebar/clip-panel/audio-eq-panel-content.tsx`). Column position stays consistent across all 5 row-groups (band N always lands in the same sub-row/column). The separate wider `BandCard`-based layout (used outside the narrowest sidebar case, with its own `isDetachedLayout` horizontal-scroll handling) was left untouched — not part of this bug |

Verified working during the sweep (1280×800, Chrome):
- Audio EQ + mixer: panel renders (6 bands, presets, band on/off toggles, filter-type menus, freq/gain/Q inputs, output gain); field edits commit to the item (`audioEq*` fields via `updateItem`, verified end-to-end); Audio section gain + fade-in/out + pitch controls present; master METERS panel renders (L/R, dB scale). Caveat: EQ number inputs too narrow — see #10. ✅
- Project bundle import/export: round-trip verified end-to-end via the snapshot path — export produces a valid schema-versioned snapshot, `validateSnapshotData` passes, `importProjectFromJsonString` creates a new project, and the re-exported imported project is an exact match of the original (`text:0:90 "Round Trip"` + `shape:100:90 star`, 2 items / 2 tracks, all positions/durations/content preserved). UI entry points present (Export → Download Project .zip; Projects → Import). The .zip bundle path shares the same convert/restore + checksum logic (packs media files on top). ✅
- Settings + hotkey editor: Settings dialog all 4 tabs render (General: auto-save/interval/undo-depth/interface-sounds; Timeline: snap/waveforms/filmstrips; AI: caption interval + caption-style presets; Storage: proxy generate/delete, cache clear, thumbnail regen, local-AI model cache management). Hotkey editor: visual keyboard with bound-key highlighting, command search, customized/conflicts/unassigned filters, 9 categories, Import/Export presets, per-command Record/Unbind/Reset — rebind capture verified (Record → pressed P → shortcut updated to "P" with Save/Cancel + LISTENING state; cancelled to preserve Space). ✅
- Render queue + export: export dialog complete (Video/Audio type, range, green preflight validation "2100 frames / codec supported", presets Max/Recommended/Balanced/Small, format/codec/quality/resolution, subtitle burn-in). Ran a real export — 70s @1080p H.264 → **playable 13.81 MB MP4 in 24s**, plays back correctly in the inline result player with video + star shape composited. Render-queue panel opens (Render queue / Saved exports tabs, "renders to workspace folder" guidance, refresh, correct empty states). ✅
- Scene browser (Media → Scenes): panel renders — keyword + semantic search modes, media/type/effect filters, sort, list/grid toggle, "0 clips · 0 scenes" empty state with clear guidance ("Run Generate Captions… to generate searchable scene captions"). UI + empty state verified; end-to-end semantic search over captioned media NOT driven this session (needs an AI caption+index run — the caption-generation flow was separately verified to start). ✅ (partial — UI only)
- Shapes + masks: all 8 shape types available (rect/circle/triangle/ellipse/star/polygon/heart/pen); Star added → renders in preview with full transform gizmo. Pen tool activates path-draw mode and accepts points. Masking model = a shape with `isMask:true` mattes the tracks below (clip/alpha, feather, invert); verified end-to-end — a star flagged `isMask` over the video composited a correct star-shaped cut-out in the preview via the gpu-masks pipeline. ✅
- **Placement gripe fixed:** new text/shapes were landing at the END of existing content instead of at the playhead. Root cause: `handleAddText`/`handleAddShape` (`media-sidebar.tsx`) picked a track by type-compatibility only (`findCompatibleTrackForItemType`), with no regard for whether the playhead position was free on it — a collision there fell back to `findNearestAvailableSpace`, which snaps forward to the nearest free edge (the end of whatever clip occupies the playhead). The canvas-drop flow (`use-canvas-media-drop.ts`) already had the correct fix — text/shapes are overlays, so they always get their **own new track** above existing content at the exact playhead frame (via `resolveOverlayLayerAnchor` + `createNewVideoZoneTrack` + `addItemOnNewTrack`, one undoable step). Applied the identical pattern to the button-click flow; moved `resolveOverlayLayerAnchor` out of the hook into the shared `new-track-zone-media.ts` util (now unit-tested) so both flows share it. Verified in-browser: with an existing clip at frames 0–60 and the playhead at frame 30, clicking Add Text / Add Shape now places the new item at `from: 30` on a fresh track above, instead of `from: 60` on the shared track. ✅
- Source monitor: open via media double-click, scrub strip, mark in/out (I/O + buttons), V/A patch targets, insert edit (`,`) with straddle-split + downstream shift, overwrite edit (`.`) with split/trim/remove of overlapped clips, linked A/V item creation, playhead advance. ✅
- Transitions: all 44 registered presentations (all now GPU-backed after the #9 wipe-mask + aperture-mask ports; each keeps a Canvas-2D `renderCanvas` fallback for non-WebGPU — CLAUDE.md's "13" is stale) apply and render mid-cut with zero console errors; click-to-apply (after fix #4), drag descriptor wiring, transition inspector (preset picker + search, duration slider/reset, placement L/C/R with handle-based disabling, ease, direction, per-transition params, delete), auto-heal after overwrite (after fix #5). ✅
- Edit workspace: import (17MB 1080×1920 mp4) → thumbnail, timeline drop (linked A/V + waveform), scrub, preview render (correct pillarboxing), clip selection/properties.
- Color workspace: live RGB parade scopes, wheels/curves render, effect picker.
- Animate workspace: transform gizmo, keyframe editor (Sheet/Graph/Split), preset library.
- **Keyframes deep pass (A1 sweep — last item): no bugs found, feature is solid.** Verified end-to-end on a text item's X-position (2 keyframes -600→+600): interpolation renders correctly while **scrubbing** (frame 0 = left / 60 = centered / 119 = right) AND during **playback** (paused mid-play at frame 21 showed the animated position, editor playhead stays synced). Sheet mode shows the Transform group with live interpolated value readouts; Graph mode draws the value curve; Split shows both. Changing a keyframe's easing (linear → ease-in-out) re-shapes the Graph curve *and* changes the rendered value correctly (frame 21 read -526 vs. linear's -390 — ease-in slow-start confirmed numerically). Motion path renders in the preview gizmo. UI "Toggle keyframe at playhead" adds a keyframe capturing the live interpolated value and toggles it back off (delete) correctly. Per-property rows expose auto-key / lock / prev-next / clear / show-curve, all labeled. Interpolation presets (Hold/Linear/Ease In/In-Out/Out) + copy/cut/paste/remove present. Zero console errors throughout. Core interpolation/easing/resolver math is separately unit-tested (101 keyframe test files). Note: firsthand-confirmed bug #6 while driving this — the left-rail panel-switcher icons (`button` refs) have no accessible names. ✅
- Export end-to-end in the UI: preflight → render 845 frames in 25s (~realtime 1080p H.264) → 29.68 MB MP4 with inline preview player. ✅

Notes:
- Supported window width: treat ~1024px as the practical minimum (pro NLE layout); audit target sizes are 1280 / 1440 / 1920. At 700–900px nothing overlaps after fix #1; the layout compresses gracefully.
- **A1 feature sweep is now COMPLETE** — every feature area has been driven end-to-end (text/captions, audio EQ, transitions incl. the #9 GPU port, masks/shapes, scene browser, source monitor, render queue, settings, hotkey editor, project bundle import/export, and keyframes). Fixed this session in addition to #9: #6 (aria-labels), #10 (EQ input width), and the text/shape-at-playhead placement gripe. Open code-work items remaining: bug #8 (paused-preview repaint, needs a real repro) and #2/#3/#7 (A2-scoped responsive/leak items, deliberately deferred to the A2 UI overhaul).
