# Bug log — Phase A1 sweep

Working list for the A1 bug sweep (MASTERPLAN §5). One line per bug: status, where, what, fix.
Priority order: data loss > broken feature > visual.

| # | Status | Area | Bug | Fix / plan |
|---|--------|------|-----|------------|
| 1 | ✅ fixed | Editor toolbar | Project title + specs line wrapped at widths ≲1000px and overflowed the fixed-height toolbar, overlapping the panel below (owner + Claude both reproduced) | `min-w-0` on the toolbar's left flex group and title column, `truncate` on the h1 and specs span, `flex-shrink-0` on the back button (`features/editor/components/toolbar.tsx`) |
| 2 | 📋 logged → A2 | Color workspace | "+ Add Effect" button label ellipsizes to "+ A…" at 1280px; Copy/Paste grade + Adjustment layer buttons wrap to two cramped rows | Bottom-row layout of the Color workspace needs a responsive pass — part of the A2 UI overhaul, not a point fix |
| 3 | 📋 logged → A2 | Color + Animate workspaces | Large unused black band (~20% of window height) below the panel row at 1280×800 — workspace content doesn't stretch to fill the viewport | Rework workspace vertical layout during A2 (panels should flex-fill) |
| 4 | ✅ fixed | Transitions panel | Click-to-apply never worked for normal footage: linked selection (default-on) selects video+audio = 2 items, and the resolver required exactly 1 selected item, so clicking a transition card silently did nothing and the hint said "select a single clip" | `resolveTransitionTargetFromSelection` now filters selection to transitionable types (video/image/composition) before the single-item check; panel hint/gate counts the same way (`transition-targets.ts`, `transitions-panel.tsx`; regression tests added) |
| 5 | ✅ fixed | Source monitor edits | Insert/overwrite edits left dangling transitions: removed or split-away clips were never reported to `applyTransitionRepairs`, so a transition could keep referencing a deleted clip id (invisible, unrepaired, saved into the project) | `performInsertEdit`/`performOverwriteEdit` now report the stale pre-split/removed item ids as affected; repair then rewires the transition to the replacement clip at the cut or drops it (`source-edit-actions.ts`; regression tests added) |
| 6 | 📋 logged | Left rail (workspace panels) | Panel-switcher icons (Media, Text, Shapes, Effects, Transitions, …) have no accessible names — read_page shows bare `button` entries; screen readers get nothing | Add aria-labels (likely via existing tooltip strings); fold into A2 UI pass or fix as a point patch |
| 7 | 📋 logged | Media decode | Repeated console errors "A VideoSample was garbage collected without first being closed" during heavy source-monitor scrubbing + rapid seeking (mediabunny resource leak — GC covers it, but decoder/VRAM pressure until then) | Audit VideoSample lifecycle in the scrub/decode path; reproduce with rapid seeks in the source monitor |
| 9 | 🔴 open | Preview (transition playback) | **Owner-reported + root-caused from `__DEBUG__.transitionTrace()` on owner's machine (spiralWipe, project uQTLsJFF).** Playback stutters so badly around the transition that it's unwatchable: one `preview_transition_session` measured **6021 ms**; rendered frame fell ~175 frames behind the clock (`currentFrame 891 / frame 716`), every frame tagged `render_frame_slow`. **Root cause: non-GPU transitions.** ~23 of the 44 presentations have `gpuTransitionId: null` (all Wipe variants incl. spiralWipe/barnDoor/split/band/center/edge/radial/x/venetian, all Iris *shapes* arrow/cross/diamond/eye/hexagon/oval/pentagon/square/triangle, all Shape box/heart/star/triangle) and render through the **CPU Canvas-2D fallback** (`CanvasTransitions.renderCanvas` via the offline `ClientRenderEngine`). During playback that path decodes each participant frame with mediabunny WASM (~40–80 ms/frame, and here repeatedly `Mediabunny frame extraction failed → using fallback`) AND rebuilds the engine every pass (`Registering shared video extractor` / `Preloading media` / `Canvas pool disposed` looping) → can't sustain framerate. GPU-backed transitions (fade, wipe, clockWipe, slide, flip, plain iris, all dissolves, glitch, pixelate, chromatic, radialBlur, sparkles, lightLeak, liquidDistort, lensWarp, filmGate) do NOT hit this and play smoothly. Preview-only — exports render offline so output is correct either way | Two fixes, pick per-transition: (a) **port the CPU-only transitions to GPU shaders** (preferred — matches the fast ones, ~23 WGSL shaders following the existing `GPU_TRANSITION_REGISTRY` pattern in `infrastructure/gpu-transitions/`); or (b) **fix the CPU fallback path** so `ClientRenderEngine` is reused across transition frames (stop re-registering extractors / disposing the canvas pool every pass) and participant frames come from the already-decoded DOM video (zero-copy) instead of mediabunny WASM re-extraction. Interim UX: mark/label the CPU-fallback transitions in the picker, or hide them until GPU-ported. Same preview subsystem as #8 |
| 8 | 🔴 open | Preview (paused) | Committed item property edits don't repaint the paused preview: edit text content in the Properties textarea (verified in UI + pixel-sampled via store `updateItem`) → the visible fast-scrub overlay keeps showing the old frame indefinitely (>5s, no repaint) until the next seek/play. The DOM Player updates invisibly *underneath* the stale overlay — exactly the failure mode described in the gizmo-store subscription comment (`use-preview-render-pump-controller.ts` ~1553), which repaints the overlay for gizmo/live-preview changes but has no equivalent for committed `updateItem` edits. The items-change invalidation effect (`use-preview-renderer-controller.ts` ~741) invalidates the frame cache + resumes the loop, but the repaint doesn't land — needs instrumented debugging | Dedicated session: instrument the invalidation effect + pump to find where the repaint request is dropped when paused with the overlay visible; mirror the gizmo-store subscription for committed item changes. Do NOT hot-patch — render-loop concurrency invariants (single-mutex pump, generation counter) are easy to break |

| 10 | 📋 logged → A2 | Audio EQ panel | Per-band number inputs (frequency/gain/Q) are unreadable: the EQ lays 6 bands in a fixed `grid grid-cols-6` at **36px/cell** inside a ~236px column, leaving each input **~14px wide** — multi-digit values clip to one digit (`22000`→"2", `1600`→"3"). Values are correct in the store and the commit path works (verified: typing 45 into band-1 freq wrote `audioEqBand1FrequencyHz:45`); it's purely that you can't read/edit them. Same responsive-layout family as #2/#3 | Rework the EQ number layout in the A2 responsive pass — don't cram 6 bands across a narrow sidebar (wrap to 2–3 rows, horizontal scroll, or per-band stack). Not a safe one-line fix |

Verified working during the sweep (1280×800, Chrome):
- Audio EQ + mixer: panel renders (6 bands, presets, band on/off toggles, filter-type menus, freq/gain/Q inputs, output gain); field edits commit to the item (`audioEq*` fields via `updateItem`, verified end-to-end); Audio section gain + fade-in/out + pitch controls present; master METERS panel renders (L/R, dB scale). Caveat: EQ number inputs too narrow — see #10. ✅
- Project bundle import/export: round-trip verified end-to-end via the snapshot path — export produces a valid schema-versioned snapshot, `validateSnapshotData` passes, `importProjectFromJsonString` creates a new project, and the re-exported imported project is an exact match of the original (`text:0:90 "Round Trip"` + `shape:100:90 star`, 2 items / 2 tracks, all positions/durations/content preserved). UI entry points present (Export → Download Project .zip; Projects → Import). The .zip bundle path shares the same convert/restore + checksum logic (packs media files on top). ✅
- Settings + hotkey editor: Settings dialog all 4 tabs render (General: auto-save/interval/undo-depth/interface-sounds; Timeline: snap/waveforms/filmstrips; AI: caption interval + caption-style presets; Storage: proxy generate/delete, cache clear, thumbnail regen, local-AI model cache management). Hotkey editor: visual keyboard with bound-key highlighting, command search, customized/conflicts/unassigned filters, 9 categories, Import/Export presets, per-command Record/Unbind/Reset — rebind capture verified (Record → pressed P → shortcut updated to "P" with Save/Cancel + LISTENING state; cancelled to preserve Space). ✅
- Render queue + export: export dialog complete (Video/Audio type, range, green preflight validation "2100 frames / codec supported", presets Max/Recommended/Balanced/Small, format/codec/quality/resolution, subtitle burn-in). Ran a real export — 70s @1080p H.264 → **playable 13.81 MB MP4 in 24s**, plays back correctly in the inline result player with video + star shape composited. Render-queue panel opens (Render queue / Saved exports tabs, "renders to workspace folder" guidance, refresh, correct empty states). ✅
- Scene browser (Media → Scenes): panel renders — keyword + semantic search modes, media/type/effect filters, sort, list/grid toggle, "0 clips · 0 scenes" empty state with clear guidance ("Run Generate Captions… to generate searchable scene captions"). UI + empty state verified; end-to-end semantic search over captioned media NOT driven this session (needs an AI caption+index run — the caption-generation flow was separately verified to start). ✅ (partial — UI only)
- Shapes + masks: all 8 shape types available (rect/circle/triangle/ellipse/star/polygon/heart/pen); Star added → renders in preview with full transform gizmo. Pen tool activates path-draw mode and accepts points. Masking model = a shape with `isMask:true` mattes the tracks below (clip/alpha, feather, invert); verified end-to-end — a star flagged `isMask` over the video composited a correct star-shaped cut-out in the preview via the gpu-masks pipeline. ✅ (Shared UX note: shapes — like text — land at the end of existing content, not at the playhead/overlay; part of the same A2 placement gripe.)
- Source monitor: open via media double-click, scrub strip, mark in/out (I/O + buttons), V/A patch targets, insert edit (`,`) with straddle-split + downstream shift, overwrite edit (`.`) with split/trim/remove of overlapped clips, linked A/V item creation, playhead advance. ✅
- Transitions: all 44 registered presentations (21 GPU + canvas-fallback variants — CLAUDE.md's "13" is stale) apply and render mid-cut with zero console errors; click-to-apply (after fix #4), drag descriptor wiring, transition inspector (preset picker + search, duration slider/reset, placement L/C/R with handle-based disabling, ease, direction, per-transition params, delete), auto-heal after overwrite (after fix #5). ✅
- Edit workspace: import (17MB 1080×1920 mp4) → thumbnail, timeline drop (linked A/V + waveform), scrub, preview render (correct pillarboxing), clip selection/properties.
- Color workspace: live RGB parade scopes, wheels/curves render, effect picker.
- Animate workspace: transform gizmo, keyframe editor (Sheet/Graph/Split), preset library.
- Export end-to-end in the UI: preflight → render 845 frames in 25s (~realtime 1080p H.264) → 29.68 MB MP4 with inline preview player. ✅

Notes:
- Supported window width: treat ~1024px as the practical minimum (pro NLE layout); audit target sizes are 1280 / 1440 / 1920. At 700–900px nothing overlaps after fix #1; the layout compresses gracefully.
- Remaining A1 work: feature-by-feature pass using the in-app user guide (26 pages) as the checklist — text/captions, audio EQ, transitions, masks/shapes, scene browser, source monitor, render queue, settings, hotkey editor, project bundle import/export.
